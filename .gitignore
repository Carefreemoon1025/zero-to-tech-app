# 依赖
node_modules/

# npm 缓存（本沙箱的家目录只读，缓存改放在项目内）
.npm-cache/

# Next.js 构建产物
.next/
out/
dist/

# 环境变量（本地/生产各不相同，一律不进版本库）
.env
.env.local
.env*.local

# 日志与临时文件
*.log
npm-debug.log*
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# 数据库文件（SQLite，运行时生成）
*.db
*.sqlite3

# 本地引导用的临时目录（下载的 deb、解包出的 pip）
.tmp-bootstrap/

# 端到端验证用的无头浏览器用户目录
.tmp-chrome*/

# 交接文档：给本机 AI agent 的工作约定，不随公开仓库发布
AGENTS.md

# 开发工具与素材（下载的源字体、gh 二进制、参考仓库、pip 引导包），不进版本库
.tooling/

# 验证过程产生的截图
.tmp-shots/
