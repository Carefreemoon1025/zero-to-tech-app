# 开发环境（next dev 时自动加载；next build 不会读这个文件）
#
# 本地是 :3000(前端) → :8000(后端) 的跨源请求，所以直接指向后端，走 CORS + cookie。
# 生产环境**故意不配**这个变量：前端与 /api 同源，由 nginx 反代，见 backend/README.md。
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
