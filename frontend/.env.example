# 后端基地址（组件里不写死域名，统一从这里读）
#
# 本地开发：前端 :3000 与后端 :8000 不同源，直接指向后端，走 CORS。
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
#
# 生产部署：留空即可 —— 前端与 /api 同源，nginx 把 /api 反代到 127.0.0.1:8000。
# 同源的好处是彻底没有跨源问题，cookie 也天然可用。
# NEXT_PUBLIC_API_BASE_URL=
