# 后端基地址（组件里不写死域名，统一从 lib/api.js 读它）
#
# 开发：放进 .env.development（已提交，next dev 自动加载）
#   NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
#
# 生产：**留空**（不配即可）。前端与 /api 同源，nginx 把 /api 反代到 127.0.0.1:8000。
# 同源的好处是彻底没有跨源问题，cookie 也天然可用。
# 如果哪天前后端真的分到两个域名，再在这里写完整地址，并同步配后端的 ALLOWED_ORIGINS。
NEXT_PUBLIC_API_BASE_URL=
